<%= simple_form_for @portfolio do |f| %>
	<%= f.input :title, label: "Mule Title" %>
	<%= f.input :description, label: "Describe your Mule (optional)" %>
	<%= f.input :content, label: "Content" %>
	<%= f.button :submit %>
<% end %>